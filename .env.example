# Stripe Payment Links for the Standard / Growth presets ($499 flat fee), one per SKU.
# Leave empty to disable checkout for that campaign type — the pay button will be
# disabled with an inline "Payment link not configured" notice, never a placeholder URL.
NEXT_PUBLIC_STRIPE_LINK_OUTBOUND_SALES=
NEXT_PUBLIC_STRIPE_LINK_SOCIAL_ORGANIC=
NEXT_PUBLIC_STRIPE_LINK_CONTENT_LED=
NEXT_PUBLIC_STRIPE_LINK_PERFORMANCE=
NEXT_PUBLIC_STRIPE_LINK_RETENTION=
